{
  "name": "ts-project-game",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist",
    "build2": "cross-env NODE_ENV=production parcel build src/index.html --public-url ."
  },
  "devDependencies": {
    "sass-embedded": "^1.83.4",
    "typescript": "~5.6.2",
    "vite": "^6.0.5",
    "gh-pages": "^1.2.0",
    "parcel-bundler": "^1.9.7"
  }
}
